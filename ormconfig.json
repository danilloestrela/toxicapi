{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "quality",
  "database": "toxic_api",
  "entities": [
    "./src/models/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": [
      "./src/database/migrations"
    ],
    "entitiesDir": "src/models"
  }
}
